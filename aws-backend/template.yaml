AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: ExoplanetHub serverless backend

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - main
    Description: Environment name

Globals:
  Function:
    Timeout: 600
    MemorySize: 512
    Runtime: python3.13

Resources:
  ExoplanetsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub exoplanets-${Environment}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pl_name
          AttributeType: S
        - AttributeName: disc_year
          AttributeType: N
      KeySchema:
        - AttributeName: pl_name
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: year-index
          KeySchema:
            - AttributeName: disc_year
              KeyType: HASH
            - AttributeName: pl_name
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  # Written by the sweep before it deletes a record the archive no longer lists.
  TombstonesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub exoplanet-tombstones-${Environment}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pl_name
          AttributeType: S
      KeySchema:
        - AttributeName: pl_name
          KeyType: HASH

  DataSyncFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub exoplanet-data-sync-${Environment}
      CodeUri: lambda/sync/
      Handler: app.lambda_handler
      Environment:
        Variables:
          TABLE_NAME: !Ref ExoplanetsTable
          TOMBSTONES_TABLE_NAME: !Ref TombstonesTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ExoplanetsTable
        - DynamoDBWritePolicy:
            TableName: !Ref TombstonesTable
      Events:
        SixHourlySync:
          Type: Schedule
          Properties:
            Schedule: rate(6 hours)
            Enabled: true

Outputs:
  ExoplanetsTableName:
    Description: DynamoDB table name
    Value: !Ref ExoplanetsTable
  ExoplanetsTableArn:
    Description: DynamoDB table ARN
    Value: !GetAtt ExoplanetsTable.Arn
  TombstonesTableName:
    Description: DynamoDB table recording planets the sweep removed
    Value: !Ref TombstonesTable
